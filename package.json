{
  "name": "emergency-ai",
  "private": true,
  "workspaces": ["apps/web", "packages/*"],
  "scripts": {"check": "npm run check --workspaces", "test": "npm run test --workspaces", "build": "npm run build --workspace @emergency-ai/web"},
  "devDependencies": {"typescript": "^5.6.0"}
}
